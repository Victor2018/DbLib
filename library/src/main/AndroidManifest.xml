<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.victor.db.library">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <provider
            android:name="com.victor.db.library.model.DataBaseContent"
            android:authorities="content.victor.movie.content"
            android:exported="true"/>
    </application>

</manifest>
